# Production-like DB-backed mode
DATABASE_URL=postgres://user:password@localhost:5432/breedlog
SESSION_SECRET=replace-with-strong-random-secret
ADMIN_PIN=replace-with-admin-pin
PORT=5000

# Local certification fallback mode (no external Postgres required)
# Enable this only for local/test verification.
USE_IN_MEMORY_STORAGE=1

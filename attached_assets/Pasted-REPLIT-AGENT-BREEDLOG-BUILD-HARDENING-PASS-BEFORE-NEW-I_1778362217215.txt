REPLIT AGENT — BREEDLOG BUILD HARDENING PASS BEFORE NEW INTELLIGENCE

Scope: harden the current BreedLog build only.

Do not add new intelligence features.
Do not redesign Data tab further.
Do not add new dashboards.
Do not run simulation seed apply.
Do not delete data.
Do not change access-code logic unless a failing test proves a regression.
Do not make broad feature changes.

Purpose:
Before we add more intelligence, stabilize the app after the latest fixes:
- access-code/admin hardening
- Data tab redesign
- image crop fix
- animal profile action button layout fix
- in-app back/forward navigation fix

HARDENING OBJECTIVES

1. Confirm app builds cleanly.
2. Confirm no TypeScript errors.
3. Confirm no broken routes.
4. Confirm no blank pages.
5. Confirm no data loss risk introduced.
6. Confirm no access-code regression.
7. Confirm no sync regression.
8. Confirm image crop works.
9. Confirm profile action buttons are correctly placed.
10. Confirm in-app back/forward navigation behaves correctly.
11. Confirm Data tab uses real data and does not render fake placeholders.
12. Confirm mobile layout remains usable.
13. Confirm PWA still loads correctly.

REQUIRED CHECKS

Run:

npm run check
npm run build
npm run test:cert

Run all targeted tests for:

- access-code/license/admin logic
- shared workspace/device sync
- local data safety
- Data tab/data-engine
- navigation
- image crop
- animal profile layout
- sync queue
- import/export if available

If test:cert has known pre-existing failures, do not hide them.
Report them separately as:
- pre-existing baseline failures
- new failures caused by current changes

Do not claim “green” if test:cert is red.

MANUAL APP CHECKLIST

Manually verify in Preview:

1. App opens without blank screen.
2. Login/access-code flow works.
3. Phone/desktop same-code logic still works in principle.
4. My Herd opens.
5. Animal profile opens.
6. Back button returns to previous page, not Home.
7. Forward behavior works where available.
8. Animal profile Edit / Remove / Refresh buttons are correctly aligned.
9. Image upload opens crop dialog.
10. Crop box is resizable/draggable.
11. Save Crop works.
12. Cancel preserves previous image.
13. Data tab opens.
14. Data tab shows real calculated metrics.
15. Bottom nav and More tab work.
16. No console/runtime errors during normal navigation.

DATA SAFETY CHECKS

Confirm:

- logout does not delete herd data
- access-code switch does not silently wipe local data
- server-empty/local-present condition does not overwrite local data silently
- slot release does not delete workspace/herd data
- revoke/reactivate does not delete workspace/herd data
- refresh/reload does not clear animals
- sync failure does not duplicate records

ROUTE CHECKS

Direct load must not crash:

/
/animals
/analysis
/data
/breeding
/health
/records
/settings
/admin
/animal/:id where valid
/breeding/:id where valid if route exists

If a route is unknown, show a proper fallback and safe navigation.

REPORT DOCUMENT

Create/update:

docs/release/build-hardening-before-intelligence.md

Include:
- what was checked
- tests run
- manual checks performed
- known failures
- fixed regressions
- remaining risks
- whether app is safe to continue to intelligence phase

COMMIT RULE

Only commit if:
- check passes
- build passes
- targeted tests pass
- no new critical regression remains

Commit message:

Harden BreedLog build before intelligence phase

FINAL REPORT

BREEDLOG BUILD HARDENING RESULT
Status:
Commit:
Files changed:
Typecheck:
Build:
Certification tests:
Targeted tests:
Manual route checks:
Data safety checks:
Crop status:
Profile button layout status:
Navigation status:
Data tab status:
Access-code status:
Known pre-existing failures:
New failures:
Remaining blockers:

FINAL STATUS:
READY TO ADD INTELLIGENCE
or
NOT READY
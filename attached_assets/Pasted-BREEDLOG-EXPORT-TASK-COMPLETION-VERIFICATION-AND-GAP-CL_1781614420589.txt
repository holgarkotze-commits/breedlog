BREEDLOG EXPORT TASK — COMPLETION VERIFICATION AND GAP CLOSURE REQUIRED BEFORE ACCEPTANCE

The first export correction task is not accepted as fully complete yet. Good progress was made on the canonical group export system, but the original Definition of Done has not been fully proven.

Do not start unrelated changes yet. First close the export-document task properly and provide full proof.

Issues from the current report that must be corrected or proven:

1. The full test suite timed out.

   * This cannot be reported as a full pass.
   * Run the full test suite again with an appropriate timeout or identify the exact slow/failing tests.
   * If the full suite genuinely cannot complete, provide the reason, the command used, the timeout limit, and targeted proof that the export changes did not break unrelated functionality.
   * Do not claim “all tests pass” if the full suite timed out.

2. Final report is missing required delivery details.
   Provide:

   * Branch name.
   * Latest commit hash.
   * Full list of files changed.
   * Exact test commands run.
   * Exact pass/fail result for each command.
   * Any skipped, timed-out, or incomplete checks.

3. Individual animal exports were not properly proven.
   The original instruction required individual animal exports to use the same canonical header/footer system in portrait layout.

   Verify and report specifically for:

   * Individual Ram export.
   * Individual Ewe export.
   * Individual Lamb export.
   * Any individual animal profile/detail PDF export.

   Confirm:

   * Portrait orientation.
   * Same canonical header.
   * Same medium logo size.
   * Same dark footer ribbon.
   * Footer fixed at the bottom of every page.
   * No content/footer overlap.
   * No inconsistent old template still being used.

4. Family tree / pedigree export was not proven.
   The original instruction required the exported family tree to reflect the same real family tree structure used in the app.

   Verify and, if not already done, implement:

   * The exported family tree must use real sire/dam lineage data.
   * No fake placeholder ancestors.
   * No generic dummy tree.
   * Missing ancestors must show the app-approved missing state only.
   * The exported tree must visually match the app’s real family tree as closely as PDF allows.
   * The tree must not overlap the footer or page margins.

   Report exactly which file/function renders the individual animal family tree export and what was changed or verified.

5. Group Rams was originally the visual source of truth, but the report says Group Rams changed from portrait, 8/page, with photos to landscape, 20/page, no photos.
   Clarify this carefully.

   The instruction was:

   * Use the correct Group Rams letterhead/header and footer ribbon as the canonical visual template.
   * Group animal exports must be landscape and support at least 20 entries per sheet.
   * Preserve correct animal-specific export data.

   Confirm:

   * The Group Rams header and footer design were preserved as source-of-truth styling.
   * The orientation and pagination were corrected to match the group-export rule.
   * No required ram data field was removed.
   * If the photo column was removed, explain why and confirm it was not a required export data field.
   * If photos are still required elsewhere, restore them or move them only where appropriate.

6. Lambs export was described as “minor mismatch”.
   That is not acceptable as a final state.

   Confirm whether the Lambs export now uses the same canonical export template as the other group exports.
   If it still has a different structure, fix it now.
   All group exports must use the same header, footer, logo sizing, landscape page setup, row/page rule, and styling system.

7. Footer proof is required.
   The report says dark footer exists, but the original defect was footer placement.

   Prove:

   * Footer Y-position is page-bottom based.
   * Footer appears at the bottom of every page.
   * Footer does not sit in the middle of any page.
   * Footer does not overlap table rows, animal detail content, or family tree content.
   * Footer is present on multipage exports, not only page 1.

8. Generate proof exports.
   Generate or manually verify sample exports for:

   * Total Herd / All Animals.
   * All Rams.
   * All Ewes.
   * All Lambs.
   * Group Rams.
   * Individual Ram.
   * Individual Ewe.
   * Individual Lamb.

   Include at least:

   * One group export with more than 20 animals to prove pagination.
   * One group export with fewer than 20 animals to prove rows/columns are not stretched.
   * One individual export with a family tree/pedigree section.

9. Add or confirm tests for all original acceptance requirements.
   At minimum, tests must cover:

   * All group animal exports use landscape orientation.
   * All individual animal exports use portrait orientation.
   * All exports use the same canonical header config.
   * All exports use the same canonical footer config.
   * All exports use the same logo sizing config.
   * Group export page size is 20 rows.
   * Footer uses the canonical dark ribbon style.
   * Footer is rendered in every canonical export document.
   * Ram export data remains ram-specific.
   * Ewe export data remains ewe-specific.
   * Lamb export data remains lamb-specific.
   * Total Herd export keeps correct herd data.
   * Individual family tree export uses real lineage data and no fake placeholder tree.
   * No old inconsistent export template remains active.

10. Re-run validation.
    Run:

npm run check
npm run build
npm test

Also run all export-specific tests.

If npm test times out again, do not hide it. Report:

* Exact command.
* Timeout duration.
* Last visible output.
* Which tests completed.
* Which tests did not complete.
* Whether this was pre-existing or caused by the export changes.

Final report required before this task can be accepted:

1. Branch name.
2. Latest commit hash.
3. Files changed.
4. Explanation of the canonical export template system.
5. Confirmation that Group Rams header/footer styling remains the visual source of truth.
6. Confirmation that all group animal exports are landscape.
7. Confirmation that all individual animal exports are portrait.
8. Confirmation that footer ribbon is fixed to the bottom of every page.
9. Confirmation that footer wording/design was not changed.
10. Confirmation that all logos use the same medium size.
11. Confirmation that group exports support 20 animal entries per page.
12. Confirmation that rows/columns do not stretch when fewer than 20 animals are exported.
13. Confirmation that Ram, Ewe, Lamb, and Total Herd data mappings were preserved.
14. Confirmation that individual family tree export reflects the app’s real family tree structure.
15. Proof export list generated/verified.
16. Test results for npm run check, npm run build, npm test, and export-specific tests.
17. Any remaining risks or blocked items.

Do not mark the export task complete until these items are closed.

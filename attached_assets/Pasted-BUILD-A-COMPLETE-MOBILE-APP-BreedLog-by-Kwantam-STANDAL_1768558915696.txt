BUILD A COMPLETE MOBILE APP: “BreedLog (by Kwantam)” — STANDALONE OFFLINE ANDROID APP

You are building a real, installable mobile application that runs fully offline on the phone. The app must be usable with ZERO internet for all core features. Only the optional AI evaluation feature uses internet and must require login.

CORE GOAL
BreedLog is an offline livestock record + Meatmaster breed classing app for sheep. It stores animals, their breed-classing scores (1–6 across 4 sections), and generates a suggestion for ram type for ewe lambs (>= 6 months). It must export all data to CSV for backups.

TECH / PLATFORM (IMPORTANT)
- Use Replit Mobile Apps flow (Expo/React Native is OK under the hood, but the app must be installable and usable as a standalone Android app).
- Offline database MUST be local SQLite (expo-sqlite). Do not rely on cloud storage for core data.
- Store all data on-device. App must work with airplane mode enabled.
- Provide a production-ready project that can be built to Android APK/AAB.
- Avoid entrypoint bugs. Ensure correct Expo entry (“main”: “expo/AppEntry”) and do not require a custom index.tsx that can cause parsing errors.
- Use ONLY relative imports (no alias like @/…) to prevent toolchain issues.

DESIGN / BRANDING (MANDATORY)
- Theme: sharp black & yellow with elegant professional finish
  - Background: #0B0B0B
  - Accent yellow: #FFC300
  - Cards: #141414
  - Body text: slightly off-white #F5F5F3
  - Headline: #FFFFFF
  - Input fields background: off-white #EDEDE9
- Clean spacing, modern rounded cards, minimal icons.
- Headings bold, body regular. If Calibri isn’t available, fall back to system fonts but keep the look close to Calibri.
- No clutter. Big buttons. Easy for a farmer.

NAVIGATION (BOTTOM TABS)
Bottom tab bar with:
1) Animals
2) Suggestions
3) AI Valuation
4) Settings
(5) Debug (only in development mode)

DATA MODEL (LOCAL SQLITE)
Create SQLite schema + data access layer.
Tables:
1) animals
- id INTEGER PRIMARY KEY AUTOINCREMENT
- tag TEXT NOT NULL (unique if possible)
- sex TEXT NOT NULL (ram/ewe/wether)
- dob TEXT (YYYY-MM-DD) nullable
- breed_type TEXT (Euro/Afro/Middle) nullable
- sire TEXT nullable
- dam TEXT nullable
- notes TEXT nullable
- created_at TEXT default datetime('now')

2) scores
- id INTEGER PRIMARY KEY AUTOINCREMENT
- animal_id INTEGER NOT NULL (FK to animals.id ON DELETE CASCADE)
- head INTEGER (1–6)
- front INTEGER (1–6)
- middle INTEGER (1–6)
- rear INTEGER (1–6)
- type TEXT (Euro/Afro/Middle) nullable
- comment TEXT nullable
- updated_at TEXT default datetime('now')

Ensure foreign keys are ON and cascade delete works.

CORE SCREENS & FUNCTIONS

TAB 1: ANIMALS
A) Animals List Screen
- Title: “BreedLog”
- Button: “Add Animal”
- List cards for animals sorted newest first
- Each card shows: TAG, SEX, Breed Type (or “Unclassified”)
- Tap card opens Animal Detail

B) Add/Edit Animal Screen
Fields:
- Tag (required)
- Sex selector (Ram / Ewe / Wether)
- DOB (YYYY-MM-DD) input
- Breed Type selector (Euro / Afro / Middle)
- Sire input
- Dam input
- Notes input (multiline)
Breed Classing Section on same screen (or separate section inside the screen):
- 4 score selectors 1–6 using big buttons/chips:
  - Head (1–6)
  - Front Quarter (1–6)
  - Middle Section (1–6)
  - Rear Quarter (1–6)
- Overall Type selector (Euro/Afro/Middle) defaults to animal type if score type blank
- Comment field (“official selection language”)
Buttons:
- Save (must save animal; if scores exist, save latest score linked to animal)
- Cancel/back

C) Animal Detail Screen
- Shows full animal fields
- Shows latest score block if exists:
  - Head, Front, Middle, Rear
  - Overall Type
  - Comment
Buttons:
- Edit (opens Edit screen)
- Delete (confirm modal; deletes animal and cascades score)

TAB 2: SUGGESTIONS (EWE LAMB SUGGESTION BOX)
Suggestion Screen
Inputs:
- Age in months (numeric)
- Current Type selector (Euro/Afro/Middle)
- Score selectors 1–6 for Head/Front/Middle/Rear
Button:
- “Get Recommendation”
Output panel:
- If age < 6: show “Not eligible: Ewe lamb under 6 months.”
- Otherwise show:
  - “Recommended Ram Type: Euro/Afro/Middle”
  - Rationale text

Suggestion Engine Logic (MUST MATCH EXACTLY)
Function recommendRamType({head,front,middle,rear,type,ageMonths}):
- If ageMonths < 6 => eligible=false, reason
- avg = (head + front + middle + rear) / 4
- rec = type ?? “Middle”
- If rear <= 3 OR middle <= 3 => rec = “Euro”
- If head <= 3 OR front <= 3 => rec = “Afro”
- If avg between 3.5 and 4.5 inclusive => rec = “Middle”
Rationales:
- Euro: “Select a Euro-type ram to add hindquarter and loin expression, depth and early growth.”
- Afro: “Select an Afro-type ram to strengthen adaptation, constitution, hooves and foraging efficiency.”
- Middle: “Use a well-balanced middle-of-the-road ram to consolidate type without over-correcting.”

TAB 3: AI VALUATION (LOGIN REQUIRED)
Purpose: Use OpenAI to generate a professional Meatmaster evaluation narrative based on the animal’s details and scores, using structured breed classing language.

Requirements:
- This tab MUST require login (user id + password / or email + password).
- Core offline app must NOT require login; only AI Valuation requires login.
- Store login session securely (expo-secure-store).
- If not logged in: show Login screen (Email/UserID + Password, Login button, Forgot password link placeholder).
- After login: user can select an animal from local database and request “AI Valuation”.
- AI request uses internet; if offline, show a friendly message: “AI Valuation requires internet. Your offline records are still available.”

AI Valuation features:
- Select an animal from Animals list (picker or search).
- Button: “Generate Valuation”
- Generate a structured evaluation with these sections (based on stored scores and notes):
  1) Head
  2) Front Quarter
  3) Middle Section
  4) Rear Quarter
  + Overall Type description (Euro/Afro/Middle)
  + Short selection recommendation (what to improve / what to keep)
- The AI response must be editable/copyable, and savable as a note linked to the animal (store locally in SQLite, new column or new table “valuations” with timestamp).
- Provide “Copy to clipboard” and “Save to Animal” buttons.

OpenAI integration:
- Use Replit’s built-in integrations/secret management (no hardcoded keys in code).
- Include a safe prompt template that uses the animal record + score values to request a professional valuation.
- Add a rate-limit / cooldown to prevent spam.
- Make the OpenAI call optional and gracefully handle errors.

TAB 4: SETTINGS
Settings Screen:
- Show app name + version
- Theme info
- Storage info: “Offline SQLite: Enabled”
- Data Management tools:
  - Export Data to CSV (IMPORTANT)
  - Import Data from CSV (optional but preferred)
  - “Reset Database” (danger zone, confirmation required)

EXPORT FUNCTIONALITY (MANDATORY)
Implement export of all animals + latest scores (or all scores) to a CSV file stored locally on the device:
- Export button in Settings: “Export CSV”
- Create CSV(s):
  - animals.csv
  - scores.csv (or a combined export, but separate is fine)
- Use a library suitable for Expo to save/share files (expo-file-system + expo-sharing).
- After export: show success message + offer “Share/Save file”.

IMPORT FUNCTIONALITY (OPTIONAL BUT STRONGLY PREFERRED)
- Allow importing animals.csv and scores.csv to restore backups.
- Validate format; show friendly errors; avoid duplicates by tag if possible.

TAB 5: DEBUG (DEV ONLY)
A Debug screen visible only in development mode that runs tests and shows pass/fail:
Logic tests (4):
1) weak middle/rear => Euro
2) weak head/front => Afro
3) balanced 4s => Middle
4) age 5 months => not eligible
SQLite integration tests:
- insert animal -> id
- fetch animal matches
- insert score -> id
- fetch score matches
- delete animal
- confirm cascade delete removed score
Provide “Run All Tests Again” button.

QUALITY REQUIREMENTS
- No crashes. Defensive input validation.
- All screens must load even if DB is empty.
- Handle date fields safely (store as text).
- Clear user-friendly messages.
- Make UI fast and simple.
- Ensure the app is buildable for Android as a standalone offline app.

DELIVERABLE
Generate the full working project in Replit Mobile Apps with:
- Complete UI
- Complete SQLite storage
- Export CSV working
- AI Valuation with login gate and OpenAI integration (via Replit connectors/secrets)
- Clear run/test instructions
- Build instructions for producing an Android APK/AAB

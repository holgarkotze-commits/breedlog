BREEDLOG REPLIT FINALIZE ONLY — PUSH COMPLETED DESKTOP NAVIGATION / FAMILY TREE / A4 EXPORT WORK

This instruction is for REPLIT only.

Do not write new feature code.
Do not change unrelated files.
Do not modify branding, AI, health, genetics logic, admin, telemetry, lamb/ram logic, or app navigation beyond the already completed desktop back/forward buttons.
Do not merge.
Only push the completed local/checkpoint work to GitHub and open the PR.

Completed local work to finalize:

1. Desktop back/forward navigation buttons in the sidebar.
2. Individual animal PDF export includes a visual branching family tree matching the in-app family tree structure.
3. A4 export fit improvements for portrait and landscape PDF output.

Required GitHub delivery:

1. Check state:
   git status --short
   git branch --show-current
   git rev-parse HEAD
   git remote -v

2. Create or use branch:
   desktop-navigation-familytree-a4-export-fit

3. Ensure only intended changes are included:

* Layout/sidebar back-forward buttons
* AnimalDetail individual export visual family tree
* A4 portrait/landscape export fit CSS
* related tests only

4. Commit:
   Add desktop navigation controls and visual family tree PDF export

5. Push branch to GitHub.

6. Open PR:
   Base: main
   Head: desktop-navigation-familytree-a4-export-fit
   Title: Add desktop navigation controls and visual family tree PDF export

7. Monitor GitHub Actions.

8. If CI fails, fetch logs and fix only the exact failing cause.

9. Repeat until CI passes.

Required final report:

* branch name
* latest commit hash
* files changed
* PR link
* GitHub Actions run link
* CI result
* npm run check result
* npm test result
* confirmation desktop back/forward buttons are present
* confirmation PDF export contains visual family tree
* confirmation A4 export fit changes are included
* confirmation no unrelated changes were made

Do not claim complete unless PR is open and CI passes.
Do not merge.

BREEDLOG REPLIT FINALIZE TASK — PUSH ADMIN ACTIVITY DASHBOARD TO GITHUB AND OPEN PR

This instruction is for REPLIT only.

Do not ask Haka to do any manual GitHub work.
Do not ask Haka to create a branch.
Do not ask Haka to open a PR.
Do not ask Haka to run GitHub Actions.
Do not ask Haka to merge.
Do not start new feature work.

You already implemented and locally verified the Admin User Activity / Testing Dashboard and activity telemetry work.

Now complete the GitHub delivery cycle automatically.

Required final actions:

1. Check current git state:
   git status --short
   git branch --show-current
   git rev-parse HEAD
   git remote -v

2. Preserve all current local changes.

3. If the current changes are not already on a feature branch, create/switch to:

admin-user-activity-dashboard

Do not lose local changes.

4. Commit all Admin Activity Dashboard work with a clear message:

Add admin user activity dashboard

5. Push the branch to GitHub:

admin-user-activity-dashboard

Use the existing GITHUB_TOKEN secret if required.
Do not print, echo, or expose the token.

6. Open a GitHub PR:

Repo:
holgarkotze-commits/breedlog

Base:
main

Head:
admin-user-activity-dashboard

Title:
Admin user activity dashboard

Body must include:

* summary of telemetry tables/routes
* summary of Admin Panel UI additions
* summary of privacy safeguards
* local verification results
* note that CI must pass before merge

7. Confirm GitHub Actions starts.

8. Monitor GitHub Actions until completion.

9. If CI fails:

* fetch the failing logs
* fix only the failing cause
* commit
* push
* rerun/allow CI to rerun
* repeat until CI passes

10. Provide final proof report only after CI passes.

Required final proof report:

* branch name
* latest commit hash
* files changed
* PR link
* GitHub Actions run link
* GitHub Actions result
* npm run check result
* npm run build result
* npm test result
* confirmation that Admin Panel shows User Activity & Testing
* confirmation that app_open telemetry records activity
* confirmation that heartbeat updates last seen/time spent
* confirmation that export/download activity is tracked
* confirmation that non-admin users cannot access admin activity routes
* any remaining limitations

Completion rule:
Do not claim complete unless:

* branch is pushed to GitHub
* PR is opened
* GitHub Actions passes
* Admin activity dashboard is present
* admin-only protection is verified

Do not merge the PR unless explicitly instructed after the proof report.

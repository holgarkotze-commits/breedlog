BreedLog sync issue — strict targeted fix (do not change anything else)

Problem:
Using the same invite code (VK7GZTF7), mobile shows existing animals correctly, but desktop shows zero animals.

This proves:

* Activation works
* Device slots work
* BUT the two devices are not linked to the same data workspace

Root issue:
The system is not binding both devices to the same persistent tester/workspace identity after code redemption.

Required fix (strict scope):

1. Ensure that invite code redemption maps BOTH devices to the SAME workspace_id / tester_id.

2. When a second device (desktop or mobile) uses a code that already has an activation:

   * DO NOT create a new workspace
   * DO NOT create a new dataset
   * Instead: attach that device to the EXISTING workspace linked to that code

3. There must be ONE source of truth:

   * animals table must be linked to workspace_id (or equivalent)
   * both devices must read/write to the SAME workspace_id

4. Fix activation logic:
   Current (wrong):

   * device activation creates isolated data context

   Required:

   * device activation attaches to existing workspace if code already has one

5. Verify database relationships:

   * invite_codes → workspace_id (or tester_id)
   * activations → device + workspace_id
   * animals → workspace_id

6. Sync behavior:

   * when desktop loads → fetch animals by workspace_id
   * when mobile loads → fetch animals by SAME workspace_id

7. Do NOT modify:

   * UI design
   * herd logic
   * classification
   * reporting
   * anything unrelated to workspace binding

Testing required:

Test 1:
Use existing code VK7GZTF7 on mobile (already has data)

Test 2:
Login on desktop using same code

Expected:
Desktop must load SAME animals as mobile

Test 3:
Add animal on desktop → sync

Expected:
Mobile must show it

Test 4:
Add animal on mobile → sync

Expected:
Desktop must show it

Test 5:
Reload both devices

Expected:
Data persists and matches

Final output required:

* Confirm root cause (workspace mismatch)
* Confirm fix applied
* Confirm both devices now share SAME dataset
* Confirm no duplicate workspaces are created per device

Do not make unrelated changes.
Keep this fix strictly scoped to workspace/data linking.

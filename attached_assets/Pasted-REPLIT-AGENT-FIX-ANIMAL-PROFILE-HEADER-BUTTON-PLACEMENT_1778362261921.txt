REPLIT AGENT — FIX ANIMAL PROFILE HEADER BUTTON PLACEMENT ONLY

Scope is strictly limited to the animal profile/detail page header action buttons.

Do not change Data tab.
Do not change access-code logic.
Do not change image crop logic unless required by button layout.
Do not change exports.
Do not redesign unrelated pages.
Do not delete data.
Do not commit if tests fail.

PROBLEM

On the animal profile/detail screen, the top-right action buttons are badly positioned.

In the attached screenshot, the Refresh button appears too high/right and overlaps the top browser/app edge area. It looks broken and unprofessional.

The visible buttons are:

- Edit
- Remove
- Refresh

The Refresh button must not float into the browser/header edge.
It must align neatly with Edit and Remove inside the app content area.

REQUIRED FIX

Fix the layout of the animal profile/detail page action buttons so that:

1. Edit, Remove, and Refresh sit in one clean action row.
2. Buttons stay inside the app page/container.
3. Buttons do not overlap the browser/PWA top bar.
4. Buttons do not overflow on desktop, tablet, or mobile.
5. On small screens, buttons should wrap cleanly or become a compact action menu.
6. Spacing must be consistent.
7. The layout must look polished in both light and dark mode.
8. Button text/icons must remain readable.
9. The Refresh action must still work exactly as before.

LIKELY FILES TO INSPECT

Check the animal detail/profile page files, likely:

- client/src/pages/AnimalDetail.tsx
- any shared animal profile header component
- layout/header/action button classes used on that page

IMPLEMENTATION EXPECTATION

Use proper responsive layout:

- action row container with flex
- justify-end or responsive wrapping
- gap spacing
- max-width/content container alignment
- no absolute/fixed positioning unless truly necessary
- no negative margins
- no top/right hardcoded offsets that cause overlap

If mobile width is tight, use:

- primary visible buttons + More actions menu, or
- clean wrapped button row

Do not hide critical actions completely.

TEST / VERIFICATION

Run:

npm run check
npm run build
npm run test:cert

Manual preview required:

1. Open animal profile/detail page.
2. Confirm Edit, Remove, Refresh align cleanly.
3. Confirm no button overlaps the browser/PWA top area.
4. Confirm layout works on mobile-width preview.
5. Confirm Refresh still works.
6. Confirm dark/light mode readability if theme is available.

COMMIT MESSAGE

Fix animal profile action button layout

FINAL REPORT

ANIMAL PROFILE ACTION BUTTON LAYOUT RESULT
Status:
Files changed:
Root cause:
Layout fix:
Refresh button status:
Mobile behavior:
Tests run:
Manual checks:
Remaining blockers:

FINAL STATUS:
READY FOR FIELD RECHECK or NOT READY
REPLIT — BREEDLOG PRODUCTION SIMULATION SEED + BUILD HARDENING + SYNC

We now need the simulation seed applied to PRODUCTION, not only dev.

Keep this narrow. Do not change unrelated app logic.

TASK 1 — PRODUCTION SEED FOR U2A2ZAVQ ONLY

First do a read-only production DB check.

Confirm in production:

- U2A2ZAVQ exists
- code status
- expiry date
- linked user/workspace/account/farm
- current animal count
- whether BL-SIM-2025-RC1 already exists
- whether any other workspaces would be touched

If production U2A2ZAVQ does not exist or does not map to one clear workspace, stop and report.

If production U2A2ZAVQ exists and maps to one clear workspace, apply the same controlled simulation seed to PRODUCTION ONLY for code:

U2A2ZAVQ

Use the existing idempotent seed logic.

Do not seed dev again.
Do not seed all users.
Do not touch any other access code.
Do not wipe existing data.
Do not overwrite real records unless they are clearly the same BL-SIM-2025-RC1 seed batch.

After seed, verify:

- animal count before
- animal count after
- ewe count
- ram count
- lamb count
- mating group count
- BL-SIM-2025-RC1 count
- other workspaces unchanged
- rerun/idempotency check does not duplicate

TASK 2 — HARDEN CURRENT BUILD STATUS

After production seed, run:

npm run check
npm run build

Run the targeted tests for the recent work:

NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/breedlog-simulation-dataset.test.ts
NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/import-export-hardening.test.ts
NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/image-crop-geometry.test.ts
NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/access-code-workspace-switch.test.ts

Also run:

npm run test:cert

If test:cert still has the known 5 baseline failures, do not block the production seed result. Report them separately as baseline failures. Do not call full release green until they are cleaned.

TASK 3 — SYNC TO GITHUB

After production seed is verified and the current code/tests are stable:

- check git status
- confirm branch
- confirm latest commit/checkpoint
- push/sync to GitHub if Replit allows it
- report whether GitHub is now updated

Do not claim GitHub Desktop is synced from Replit. Just report the GitHub commit/branch so we can pull it into GitHub Desktop locally.

FINAL REPORT ONLY

PRODUCTION SEED RESULT
Code:
Production workspace:
Animal count before:
Animal count after:
Seed batch:
Idempotency:
Other workspaces touched: yes/no

BUILD HARDENING RESULT
npm run check:
npm run build:
targeted tests:
npm run test:cert:
known baseline failures:

GIT SYNC RESULT
Branch:
Commit/checkpoint:
Pushed to GitHub: yes/no
Next action for GitHub Desktop:
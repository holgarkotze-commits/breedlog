TECHNICAL FIX DIRECTIVE: BREEDLOG WEB/DB SYNC & DEVICE LOCK
PHASE 1: DEVICE ACCESS & AUTHENTICATION ENFORCEMENT
Goal: Fix the "One Code = One Device" rule and ensure the Web App recognizes the active session correctly.

Strict Device Hardware ID: Implement a fingerprinting method to identify the device.

If a code is used, it must be bound to a device_id in the database.

The Fix: On the login attempt, check if the code is already bound to a different device_id. If yes, reject login with: "This code is active on another device. Please log out there first."

Session Heartbeat: Ensure that when a user closes the browser tab, the "active" status is cleared or has a short timeout (5 mins) so they aren't locked out of their own code forever.

PHASE 2: REPAIRING THE WEB APP DATA PIPELINE (CRUD FIX)
Goal: Stop the "Fake Save" loop. The app must not say "Saved" unless the Database returns a 200 OK.

Disable "Optimistic UI" for Web: On the Web App, disable the feature that updates the UI before the server responds.

Synchronous Write Enforcement:

Modify saveAnimal() function.

Requirement: The function must await the API response.

Logic: ```javascript try { const response = await api.post('/animals', data); if (response.status === 200) { triggerToast("Saved Successfully"); refreshAnimalList(); // Force UI Reload } } catch (error) { triggerToast("Sync Failed: Record not saved to cloud."); }

Fix Connection Sensing: The message "Will sync when online" appearing while the PC is online indicates a broken navigator.onLine check.

Requirement: Replace simple browser checks with a "Hard Ping" to the actual database/API endpoint to verify true connectivity.

PHASE 3: STATE MANAGEMENT & UI REFRESH
Goal: Ensure that once data is saved, the "Zero Animals" count updates to "1".

Global State Sync: Verify that the AnimalList component is actually listening to the database and not just a local variable that resets on refresh.

Data Scope Check: Ensure the Web App is passing the correct FarmID or OwnerID in the header. If the Web App is sending a "Null" ID, the data is being saved into a "black hole" and won't show up in the list.

PHASE 4: RIGOROUS TESTING & LOGGING (VERIFICATION)
Goal: No more guessing. We need to see the "wires."

Verbose Console Logging: Enable logs that show:

[AUTH] Attempting login with code: XXXX

[SYNC] Attempting to save RAM. Data payload: {...}

[SERVER] Response: 403 Forbidden / 500 Error / 200 Success

The "Refresh" Test: * Step A: Add Animal.

Step B: Success message appears.

Step C: Hard Refresh (F5).

Success Condition: The animal MUST still be there. If it disappears, the write to the Database failed.

📋 REPLIT AGENT INSTRUCTIONS (Copy & Paste this)
Agent, follow these instructions to fix BreedLog Web App immediately:

DEVICE LOCK: Update the Auth logic. One code can only have one active session_token associated with one device_id. If a second device tries to use the same code, return a 403 error.

FIX WEB SAVING: The Web App is showing a "Saved" toast but the database insert is failing or being blocked.

Check supabase or database connection strings for the Web environment.

Ensure Animal inserts are not being queued for "Offline Sync" when the browser is clearly online.

Force a refetch() of all animal counts immediately after the POST request returns success.

DATA INTEGRITY: Ensure the Web App is sending the farm_id with every request. If farm_id is missing, the UI will show 0 animals because it doesn't know which herd to load.

TESTING: You must provide a log showing a successful POST request to the animals table followed by a successful GET request that returns a count higher than 0.
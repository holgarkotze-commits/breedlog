TASK: Fix beta access activation + device authentication so it works 100% reliably on MOBILE + DESKTOP (online/offline behavior consistent). We are seeing:
- 401 {"message":"Device not registered"}
- 500 {"message":"Device not authenticated"}
Testers enter valid invite code but activation fails unpredictably. Must be deterministic.

NON-NEGOTIABLE REQUIREMENTS
1) Activation must be ATOMIC:
   - When user enters invite code and taps "Activate Access", the server must:
     a) Validate invite code exists + is active + has remaining uses + not expired
     b) Create or update device registration record for THIS browser/device
     c) Issue a deviceAuth token (or signed session) and return it
     d) Mark invite code as used ONLY AFTER device registration + token issuance succeed
   - If any step fails, do NOT consume the invite code. Return clear error message.

2) Persist device auth consistently (no fragile cookies only):
   - Store device token + deviceId in localStorage (or IndexedDB) after activation.
   - Every API call after activation must send credentials (Authorization header or X-Device-Token header).
   - This must work the same on desktop + mobile browsers.

3) Fix service worker/caching issues:
   - Ensure service worker NEVER caches these routes:
     /api/device/*, /api/beta/*, /api/auth/*, /api/admin/*
   - Add Cache-Control: no-store, no-cache, must-revalidate headers on ALL auth/device/beta/admin endpoints.
   - Client fetch must include cache: "no-store" for these endpoints.

4) Improve user-facing errors:
   - Replace raw JSON errors with friendly text:
     - "Activation failed — please refresh and try again."
     - "Code invalid/expired/used."
     - "Network issue — try again."
   - Log full error details to server logs.

5) Offline behavior must be correct:
   - First-time activation requires internet.
   - After successful activation once, app should open in airplane mode using local data.
   - If device token exists locally, do not block app in offline mode.

6) Add diagnostics for support:
   - Add endpoint: GET /api/device/status
     returns:
       { hasTokenOnRequest, deviceId, isRegistered, tokenValid, serverTime, env/dbName }
   - Add small hidden "Diagnostics" button on beta access screen (or long-press logo) that shows deviceId + last activation time + app version/hash.

ACCEPTANCE TESTS (must be executed and confirmed)
A) New tester on Android Chrome:
   - Enter active code -> activation succeeds first try
   - Refresh page -> stays activated
   - Close browser, reopen -> stays activated
B) Desktop Chrome/Edge:
   - Same as above
C) Offline test:
   - Activate online once
   - Turn on airplane mode
   - App must open and allow viewing/adding animals locally (no forced re-activation)
D) Negative cases:
   - Expired/used/revoked code shows correct message and does not register device.

DELIVERABLES
- List exact files changed
- Confirm headers added
- Confirm service worker bypass/caching rules
- Provide test evidence notes for A–D above
- Republish to production (breedlog.replit.app) after changes

Implement a “Records” filing system redesign and header layout fixes across the app.

========================================
A) MOVE “CULLED” OUT OF MY HERD → INTO RECORDS
========================================
- Remove the Culled tab from “My Herd”.
- Add a “Culled” folder inside the “Records” tab.
- All culled animals must be stored and accessed ONLY via:
  Records → Culled (folder)

The culled list must remain searchable, filterable, and exportable, but must not appear in active herd lists.

========================================
B) REDESIGN “RECORDS” TAB (MUST NOT LOOK LIKE MY HERD)
========================================
Currently Records looks similar to My Herd. Change Records into a folder-based filing system.

Records must show a list/grid of folders (no animal herd lists on the main Records screen).

Each folder opens into a clean list/table view with filters + export options.

RECORDS HOME (FOLDER VIEW):
Create the following folders (minimum):

1) Culled
   - Contains: all culled animals (confirmed culled only)
   - Exports: Culled Export PDF

2) Sold / Removed
   - Contains: animals removed for reason “sold” or “transferred”
   - Exports: Sold/Removed Export PDF

3) Deceased
   - Contains: animals removed for reason “deceased”
   - Exports: Deceased Report PDF

4) Exported Documents
   - A filing area for ALL generated/exported PDFs
   - Documents must be automatically filed into subfolders by document type:
     - Herd Exports
     - Individual Animal Exports
     - Mating Group Reports
     - Culled Reports
     - Sold/Removed Reports
     - Productivity Logs (see below)

5) Productivity Logs
   - Contains ONLY productivity-related records/logs (not herd lists)
   - Examples of logs to store/export here:
     - Weight logs (100-day, 270-day)
     - Lambing outcomes per ewe
     - Mating history summaries
     - Weaning summaries
   - Exports: Productivity summary PDFs and/or CSV (if available)

RULES:
- Records must feel like a “filing cabinet” / “document archive”
- Easy navigation, clear folder names, and clean layout
- Include search + filters within each folder’s list
- Add a breadcrumb path (e.g., Records > Culled) for easy back navigation
- Do NOT show animal profile image columns in Records tables

========================================
C) EXPORTED DOCS AUTO-FILING (USER FRIENDLY)
========================================
Whenever a PDF export is generated anywhere in the app:
- Automatically save a copy in: Records → Exported Documents → correct subfolder
- File naming convention:
  [DocType]_[Group/AnimalID]_[YYYY-MM-DD].pdf
Examples:
  HerdExport_FullHerd_2026-01-28.pdf
  AnimalRecord_EWE-101_2026-01-28.pdf
  MatingGroup_SummerJan03_2026-01-24.pdf

Allow user actions on saved documents:
- View
- Download/Save
- Delete
- Share (if supported)

========================================
D) UI FIX: FARM NAME CROPPING (ALL SCREENS)
========================================
Audit ALL screens (web + mobile) and fix the farm name display so it is NEVER cropped.

SPECIFIC HEADER POSITION RULE:
- Keep the logo and ribbon unchanged
- Reposition the FARM NAME so it appears clearly ABOVE:
  - the “Export” button
  - the “Add Animal” button
- Ensure correct padding/margins and responsive scaling so it fits on:
  - mobile portrait
  - mobile landscape
  - tablet
  - desktop web

This must be tested on multiple screen sizes to confirm no cropping or overlap.

========================================
E) TESTING REQUIREMENTS (MANDATORY)
========================================
1) Confirm Culled animals no longer appear under My Herd.
2) Confirm Records shows folders (not herd-like tabs/lists) on the main Records screen.
3) Confirm each folder opens correctly and lists only relevant records.
4) Confirm all exports are automatically saved under Records → Exported Documents → correct subfolder.
5) Confirm farm name is not cropped or overlapping on any screen size; logo and ribbon unchanged.

BREEDLOG - REQUIRED FIXES (PDF EXPORTS + UI + PERFORMANCE) — follow instructions exactly, no feature creep.

GOAL:
1) Exported PDFs must be MUCH smaller file size (e.g., not 50MB+), fast to generate, and readable.
2) All PDF layouts must have correct padding/page breaks and footer on EVERY page.
3) Individual animal record PDFs must include key fields that exist in the Full Livestock Register.
4) Mating Group exports must export ONLY the selected group (one group per PDF), and include the ram image next to ram specs.
5) Images in the app must support pinch-zoom on mobile.
6) Herd list exports must show row numbering (1,2,3…) per category.
7) Fix missing Export button behavior in the “Rams” minimized ribbon.
8) Confirm offline functionality still works 100% after changes.

========================================
A) PDF SIZE OPTIMIZATION (CRITICAL)
========================================
Problem: “Kwantam Meatmasters – Herd Export.pdf” exported at ~51MB. Too big; slows app and sharing.
Requirements:
1) Reduce PDF size drastically while keeping readability:
   - Compress images used in PDFs (convert to JPEG, quality target ~70–80%, max width e.g. 1200px).
   - Avoid embedding original full-resolution photos directly.
   - Use efficient PDF generation settings (no duplicate embedded fonts, avoid base64 bloat).
2) Keep tables/text crisp and readable.
3) Verify exported PDFs are reasonably small for 100–500 animals.

Acceptance test:
- Export a “Full Herd Register” with sample images and confirm file size is reduced significantly (target: single-digit MBs, not 50MB+).

========================================
B) FOOTER + PAGE BREAKS + PADDING (ALL EXPORTS)
========================================
Problem: Content spills across pages incorrectly; footers not consistent; page 2 sometimes starts at top without proper padding and footer placement.
Requirements:
1) Every exported PDF page must have:
   - Top padding/margin
   - Bottom margin reserved for footer
   - Footer anchored to the bottom of each page (not directly after content)
2) When a table/section reaches bottom, it must break cleanly:
   - Stop content above footer area
   - Continue content on next page WITH correct top padding
3) Apply this across ALL exports: herd register, individual animal record, mating groups, etc.

Acceptance test:
- Generate an export that spans multiple pages and verify:
  a) footer appears on every page
  b) no text overlaps footer
  c) page 2+ content begins with correct top padding/margins

========================================
C) FOOTER READABILITY (TEXT COLOR + YELLOW TEXT SHADOW)
========================================
Problem: Light grey footer text and yellow text are hard to read on white pages.
Requirements:
1) Replace light grey footer text with DARK GREY across all PDFs.
2) Add subtle shadow or outline behind yellow text so it pops on white background.
3) Apply consistently across all export templates.

Acceptance test:
- Visually verify footer + yellow headings/logos are readable on white.

========================================
D) INDIVIDUAL ANIMAL RECORD MUST INCLUDE REGISTER FIELDS
========================================
Problem: “Complete Livestock Register” shows ewe performance fields, but “Individual Animal Record” does not.
Requirements:
1) For EWE individual record PDF, include ALL relevant fields shown in the register ribbon, such as:
   - First Lamb Date
   - Total Lamb Count
   - ILP (days)
   - Weaned Lamb Count
   - Avg Wean Weight (kg)
   - Any other ewe production fields shown in the register export
2) Ensure these appear in a clear section (e.g., “Ewe Performance / Production”).

Acceptance test:
- Open an Individual Ewe Record PDF and confirm those fields appear when data exists.

========================================
E) MATING GROUP EXPORT MUST BE INDIVIDUAL (ONE GROUP PER PDF)
========================================
Problem: When exporting a mating group (e.g., “Feb 2026 dek”), the PDF includes multiple/all groups.
Requirements:
1) Export ONLY the selected mating group (one mating group per PDF).
2) The PDF must show:
   - Mating Group name/title
   - Ram specs table
   - Ewes list (as currently)
3) Add the RAM IMAGE next to the ram specs (only ram image; do NOT add ewe images).
4) The ram image in the PDF should be same image used in app for that ram, but compressed as per PDF optimization rules.

Acceptance test:
- Export “Feb 2026 dek” group and confirm PDF contains ONLY that group and includes the ram photo.

========================================
F) MOBILE IMAGE ZOOM (IN-APP)
========================================
Problem: User cannot zoom images on mobile.
Requirements:
1) Any image opened/viewed in the app must support pinch-to-zoom and pan (mobile).
2) Must work on Android Chrome and inside PWA/webview style.

Acceptance test:
- On mobile, open an animal image and confirm pinch zoom works.

========================================
G) HERD LIST NUMBERING (RAMS/EWES/LAMBS)
========================================
Requirement:
1) In herd registers/lists, show a numbering column on the LEFT for each category list:
   - Ewes: 1,2,3…
   - Rams: 1,2,3…
   - Lambs: 1,2,3…
2) Numbering must respect current filters/sorts (i.e., number the displayed rows).

========================================
H) “RAMS” MINIMIZED RIBBON EXPORT BUTTON
========================================
Problem: Rams minimized ribbon doesn’t show export option like others.
Requirement:
- Make export button behavior consistent across Total Herd / Rams / Ewes / Lambs ribbons.
- If “Export PDF” is present for Ewes, it must be present for Rams too in the same UI state.

========================================
I) PERFORMANCE + OFFLINE CONFIRMATION
========================================
1) Ensure PDF generation does not freeze UI; show progress/loading indicator.
2) Ensure farm settings saving remains local-first and queued sync.
3) Confirm offline mode works:
   - Add animals offline
   - View lists offline
   - No crashes
   - Sync happens when online returns

========================================
DELIVERABLES REQUIRED
========================================
1) Implement all changes above.
2) Run tests and provide a checklist report:
   - Each section A–I: PASS/FAIL + short notes + what was tested
3) Confirm works on:
   - Mobile (Android Chrome)
   - Desktop browser

DO NOT add new features beyond this list. Execute exactly these requirements.

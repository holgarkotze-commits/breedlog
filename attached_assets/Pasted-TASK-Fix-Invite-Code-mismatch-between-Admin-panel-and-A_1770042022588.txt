TASK: Fix Invite Code mismatch between Admin panel and Activation validation in BreedLog production.

PROBLEM:
Admin panel shows invite codes (e.g. NJ3HCLPF) but activation endpoint returns 400 "Code not found". Replit agent also reports a different set of codes in "the database". This indicates Admin list + Activation validation are reading from different data sources (dev vs prod DB, wrong connection string, or cached/stale client state).

GOAL:
Admin UI list + Activation validation MUST use the SAME production database and the SAME table/query. Whatever Admin shows MUST always be valid for activation (unless revoked/expired/used).

STEP 1 — Confirm environment & DB identity (NO guessing):
1) In the production deployment environment, log and/or expose a safe debug readout for:
   - NODE_ENV
   - DB type (sqlite/postgres/etc)
   - DB name/file OR database host+dbname (mask password)
2) Add a read-only endpoint: GET /api/admin/db-info (requires admin auth)
   - returns: { env, dbNameOrFile, totalCodesCount }
3) Display this db-info on Admin page (small text top-right): "DB: <env>/<dbName> Codes: <count>"

STEP 2 — Ensure Admin list and Activation check same source:
1) Identify the code validation endpoint used on access screen (e.g. POST /api/device-auth/activate or /api/access/activate).
2) Identify the admin list endpoint (e.g. GET /api/admin/invite-codes).
3) Ensure BOTH endpoints use the same database connection instance and query the same table (invite_codes).
4) Remove any mock/seed/localStorage-based list rendering. Admin must render ONLY the API response.

STEP 3 — Fix caching issues (force fresh data):
1) For all admin + invite-code API responses, set headers:
   Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
   Pragma: no-cache
   Expires: 0
2) On the client fetch calls for admin list, use:
   fetch(url, { cache: "no-store", headers: { "Cache-Control": "no-cache" } })
3) Ensure service worker does NOT cache /api/* responses. Add a SW rule to bypass caching for API routes.

STEP 4 — Verify with hard acceptance tests (must pass in PRODUCTION URL):
Run these tests against https://breedlog.replit.app (production deploy), not workspace dev server:
A) Login to /admin with PIN BL3666 → Admin loads db-info and shows codes.
B) Click "Create Code" with a unique note (e.g. "TEST_DESKTOP") → code appears in list immediately.
C) In a new incognito tab, open main app access screen → paste that new code → activation succeeds.
D) Revoke that same code in admin → try activate again → must fail with "revoked" message.
E) Refresh admin page → code list must persist and match activation behavior.

DELIVERABLE:
- Explain in plain text which root cause was found (wrong DB/env vs caching vs localStorage state).
- Confirm db-info values for production.
- Confirm tests A–E PASS with screenshots or logs.

Replit Agent, final close only. Do not publish yet.

Current good state:

* npm run check: PASS
* npm run build: PASS
* npm run test:cert: PASS, 266/266
* U2A2ZAVQ reseed applied in Replit DB
* simulation animals: 1430
* normal workspace simulation leakage: 0

Now fix/verify only the remaining release blockers:

1. Commit/sync status
   Report exact:
   git status --short
   git log --oneline -5

Confirm whether these changes are committed:

* tests/runtime-certification.test.ts
* scripts/reset-master-simulation-workspace.ts
* .replit
* artifacts/field-test/breedlog-simulation-report.md

If uncommitted, commit them:
git add tests/runtime-certification.test.ts scripts/reset-master-simulation-workspace.ts .replit artifacts/field-test/breedlog-simulation-report.md
git commit -m "fix(release): harden simulation reseed and runtime certification"

2. GitHub sync
   Try Replit Git UI sync/push if available.
   If blocked, report exactly that manual GitHub sync is required before final publish.

3. Production DB blocker
   Confirm whether you can access the production DB from this Replit environment.
   If yes, apply only:
   ALTER TABLE animals ADD COLUMN IF NOT EXISTS animal_source text DEFAULT 'unknown_not_recorded';

If no, report:
PRODUCTION_DB_MIGRATION_REQUIRED_MANUALLY

4. Multi-device proof
   Prove U2A2ZAVQ really supports more than the normal 1 mobile + 1 desktop.
   Test or inspect:

* at least 3 mobile-like device activations
* at least 3 desktop-like device activations
* all resolve to same workspace userId
* normal code still blocks extra devices

If you cannot activate-test it, give direct code/database proof and mark rendered/live activation proof as pending.

5. Rerun final:
   npm run check
   npm run build
   npm run test:cert

Final report only:

* final HEAD
* working tree clean YES/NO
* GitHub synced YES/NO
* production DB animal_source ready YES/NO
* U2A2ZAVQ >2 device proof YES/NO
* normal codes still limited YES/NO
* check/build/test:cert result
* publish readiness YES/NO

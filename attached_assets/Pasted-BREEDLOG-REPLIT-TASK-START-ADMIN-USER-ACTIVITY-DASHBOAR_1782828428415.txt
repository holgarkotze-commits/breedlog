BREEDLOG REPLIT TASK — START ADMIN USER ACTIVITY DASHBOARD END TO END

This instruction is for REPLIT only.

Do not ask Haka to do manual GitHub work.
Do not ask Haka to create branches.
Do not ask Haka to open pull requests.
Do not ask Haka to run GitHub Actions manually.
Do not ask Haka to merge manually.

You must handle the full GitHub workflow from Replit using the existing GITHUB_TOKEN secret.

Current status:

* BreedLog PR #9 was merged into main.
* The work branch was deleted.
* Genetics/pedigree/bloodline work is now in main.
* Start the next feature as a new branch from latest main.

Task:
Upgrade the BreedLog Admin Panel with a user activity/testing dashboard so Haka can monitor beta/test-user engagement.

Required Git workflow:

1. Fetch latest main:
   git fetch origin
   git checkout main
   git pull origin main

2. Create a new branch:
   admin-user-activity-dashboard

3. Implement the feature.

4. Run:
   npm run check
   npm run build
   npm test

5. Push the branch to GitHub.

6. Open a PR:
   base: main
   head: admin-user-activity-dashboard
   title: Admin user activity dashboard

7. Confirm GitHub Actions starts.

8. If CI fails, fetch logs, fix the issue, push again, and repeat until CI passes.

9. Report final PR link, CI status, and any remaining limitations.

Feature goal:
The Admin Panel must show not only active users, but actual testing/use activity:

* when users last connected to the app
* when they last synced
* whether they are actively testing
* app session activity
* estimated time spent
* download/export activity
* install/PWA activity where detectable
* feature usage level
* inactive/dormant users
* recent activity trends

Privacy rule:
Track operational activity only. Do not collect private notes, passwords, tokens, animal record contents, AI prompt contents, images, GPS location, or sensitive free text.

Admin Panel UI requirements:
Add a professional compact section called:

User Activity & Testing

Add summary KPI cards:

* Total activated users
* Active today
* Active last 7 days
* Recently seen / online
* Users with sync activity
* Users with no activity after activation
* Total sessions
* Average session duration
* Export/download count
* Most active testers

Add a user activity table with:

* User / activation code
* Device type
* Status
* Activated date
* Last seen
* Last sync
* Last session start
* Last session end
* Estimated time spent
* Session count
* Activity score
* Install/download/export activity
* Last feature used
* Status indicator:

  * Active
  * Testing
  * Light activity
  * Activated but not testing
  * Dormant
  * Sync issue suspected

Add sorting/filtering for:

* last seen
* last sync
* activity score
* active today
* dormant users
* activation code
* device type

Add per-user detail drawer/modal:

* identity/device summary
* recent activity timeline
* sessions over last 7 days
* sync events
* export/download events
* install/PWA events if available
* most used app areas
* first seen / last seen
* total active time estimate
* activity warnings

Data model:
Add persistent telemetry tables or equivalent storage for:

user_activity_events:

* id
* userId
* deviceId
* eventType
* eventCategory
* route
* feature
* metadata jsonb
* occurredAt
* createdAt

user_app_sessions:

* id
* userId
* deviceId
* sessionId
* startedAt
* lastHeartbeatAt
* endedAt
* durationSeconds
* isActive
* createdAt

Optional aggregate table:
user_activity_daily:

* id
* userId
* deviceId
* date
* sessionCount
* activeSeconds
* syncCount
* exportCount
* installCount
* downloadCount
* featureUseCount
* lastSeenAt
* activityScore

Telemetry events to track:
App/session:

* app_open
* app_background
* app_foreground
* app_close where possible
* heartbeat
* route_view

Sync/connectivity:

* sync_start
* sync_success
* sync_failed
* offline_detected
* online_restored

Testing/use:

* animal_created
* animal_updated
* animal_viewed
* breeding_record_created
* health_record_created
* performance_record_created
* image_uploaded
* ai_assistant_opened
* ai_question_asked
* genetics_page_opened
* mating_risk_checked

Download/install/export:

* pwa_install_prompt_shown
* pwa_install_accepted
* pwa_install_dismissed
* export_generated
* pdf_downloaded
* spreadsheet_downloaded
* backup_exported
* report_opened

Admin:

* admin_view_opened
* admin_activity_filter_used

Frontend telemetry:
Create a small non-breaking telemetry client/hook:

* sessionId generated per app session
* app_open sent on load
* route_view sent on route change
* heartbeat every 60 seconds while document is visible
* app_background when hidden
* app_foreground when visible again
* use navigator.sendBeacon where appropriate with fetch fallback
* queue telemetry if offline and retry later
* telemetry failure must never crash the app
* do not spam backend

Backend routes:
Add authenticated user routes:

* POST /api/activity/event
* POST /api/activity/session/heartbeat

Add admin-only routes:

* GET /api/admin/activity/summary
* GET /api/admin/activity/users
* GET /api/admin/activity/users/:userId
* GET /api/admin/activity/events

Activity score:
Create 0–100 score:

* recent app open: +15
* active session in last 24h: +20
* sync success in last 7 days: +15
* record create/update in last 7 days: +20
* export/download activity: +10
* repeated sessions over last 7 days: +20
  Cap at 100.

Classify:

* 80–100: Strong tester
* 50–79: Active tester
* 20–49: Light activity
* 1–19: Activated but low use
* 0: No activity after activation

Time-spent logic:

* Start session at app_open.
* Update lastHeartbeatAt every 60 seconds while visible.
* If no heartbeat for more than 3 minutes, treat session as ended at lastHeartbeatAt.
* Do not count background time as active time.
* Store durationSeconds.

Download/install logic:
If exact browser file save cannot be detected, record the closest reliable app event:

* export generation
* download button click
* report opened
* backup exported
* PWA install accepted

Tests required:
Add tests for:

* telemetry event creation
* heartbeat update
* session timeout/duration calculation
* userId/deviceId filtering
* admin summary aggregation
* activity score calculation
* normal user cannot access admin activity routes
* telemetry failure does not crash frontend
* offline queued telemetry retries safely

Required proof report:
Return:

* branch name
* latest commit hash
* files changed
* npm run check result
* npm run build result
* npm test result
* GitHub PR link
* GitHub Actions run link
* CI status
* screenshots or runtime proof summary of Admin Panel activity dashboard
* proof that app_open creates activity
* proof heartbeat updates last seen/time spent
* proof export/download activity appears
* proof non-admin users cannot access admin activity data

Completion rule:
Do not claim complete unless:

* branch is pushed
* PR is opened
* CI passes
* Admin Panel visually shows user activity, sync status, time spent, and download/export activity
* admin-only protection is verified

Branding:
Where a footer/signoff is appropriate in admin reports or exports, use:
Developed by STITCH WORX — Software, systems & digital builds.

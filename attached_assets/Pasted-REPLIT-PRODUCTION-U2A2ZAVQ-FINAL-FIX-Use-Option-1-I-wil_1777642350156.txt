REPLIT — PRODUCTION U2A2ZAVQ FINAL FIX

Use Option 1.

I will provide the production DATABASE_URL as a Replit Secret only, not in chat.

After the production DATABASE_URL is available, do this in production only.

Do not touch dev.
Do not touch any other code.
Do not seed globally.
Do not delete the 23 legacy mobile animals.
Do not discard existing data.

TARGET CODE:
U2A2ZAVQ

PRODUCTION SITUATION ALREADY CONFIRMED:
Canonical workspace owner:
d08b57e6-82a0-45cf-b263-b9b4f9fbd193

Legacy mobile user:
055a3af0-69bb-4098-836a-1991f6e1719a

The mobile user correctly points shared_user_id to the canonical workspace, but 23 animals still exist under the old mobile user_id bucket.

TASK 1 — BACKUP / SNAPSHOT FIRST

Before any production write, create a clear production snapshot report:

- code status
- canonical workspace userId
- mobile userId
- canonical animal count
- legacy mobile animal count
- BL-SIM-2025-RC1 count
- total production animal count
- all affected table counts

Do not continue if the workspace mapping is not exactly as already reported.

TASK 2 — SAFELY UNIFY LEGACY MOBILE DATA

Move/reassign the 23 legacy mobile-user animals from:

055a3af0-69bb-4098-836a-1991f6e1719a

to canonical workspace:

d08b57e6-82a0-45cf-b263-b9b4f9fbd193

Do not delete them.

Do not overwrite conflicting animals.

If any canonical tag/EID collision exists, stop and report conflict instead of forcing it.

After reassignment, verify:

- legacy mobile user animal count = 0
- canonical workspace animal count = previous canonical count + 23
- both desktop and mobile should now resolve to the same canonical workspace

TASK 3 — APPLY SIMULATION SEED TO PRODUCTION

Apply the existing idempotent simulation seed to production only for:

U2A2ZAVQ

Use seed batch:

BL-SIM-2025-RC1

The seed must add only missing seed records.
It must not duplicate if rerun.
It must not touch other workspaces.

Expected after seed:

- existing unified real/legacy animals preserved
- 426 simulation animals added
- BL-SIM-2025-RC1 count = 426
- other production workspaces unchanged

TASK 4 — VERIFY PRODUCTION APP STATE

After production seed, verify by DB query:

- U2A2ZAVQ exists
- canonical workspace ID
- animal count before and after
- legacy mobile bucket count
- BL-SIM-2025-RC1 count
- ewe count
- ram count
- lamb count
- mating group count
- other workspaces unchanged
- rerun/idempotency check does not duplicate

TASK 5 — BUILD CHECK

Run:

npm run check
npm run build

Run targeted tests:

NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/breedlog-simulation-dataset.test.ts
NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/import-export-hardening.test.ts
NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/image-crop-geometry.test.ts
NODE_ENV=test USE_IN_MEMORY_STORAGE=1 ./node_modules/.bin/tsx --test tests/access-code-workspace-switch.test.ts

Do not block this production data fix on the known 5 baseline runtime-certification failures, but report them separately.

FINAL REPORT ONLY:

PRODUCTION U2A2ZAVQ FIX RESULT

1. Production access:
Production DATABASE_URL available: yes/no
Write access confirmed: yes/no

2. Workspace unification:
Canonical workspace:
Legacy mobile user:
Legacy mobile animals before:
Legacy mobile animals after:
Canonical animals before:
Canonical animals after unification:
Conflicts found: yes/no

3. Simulation seed:
Applied: yes/no
Seed batch:
BL-SIM-2025-RC1 count:
Final canonical animal count:
Other workspaces touched: yes/no
Idempotency rerun result:

4. Build/tests:
npm run check:
npm run build:
targeted tests:
known baseline failures:

5. Final status:
READY TO TEST PRODUCTION APP / NOT READY
Replit Agent: stop patching this in pieces. Do a proper end-to-end BreedLog Assistant repair and proof pass.

Current unacceptable result:
The BreedLog Assistant opens, but it is not truly working as live AI. It only shows:
“Live AI polish is temporarily unavailable — here is a record-based BreedLog summary…”

That is not good enough as the final state. The local record fallback is useful, but it must not become the main answer when live AI is expected to work.

Also, the assistant modal is still not properly mobile-safe. The response card is clipped/hidden behind the bottom input area, and the mobile layout still feels patched instead of professionally engineered.

Objective:
BreedLog Assistant must work as a proper mobile-first AI assistant:

* live provider AI must work when quota/key/model are valid
* local deterministic fallback must only be used when live provider is unavailable
* the app must clearly show provider status
* the mobile assistant must fit correctly without clipping, overlap, or horizontal overflow
* no raw provider errors may ever be shown to users
* do not claim complete until this is proven with tests/checks

Required root-cause audit first:
Before changing code randomly, trace the full assistant flow:

1. Identify the frontend component that sends assistant requests.
2. Identify the backend endpoint receiving the request.
3. Identify the provider client used for Gemini/live AI.
4. Identify the model name currently used.
5. Identify environment variables required.
6. Identify why the live provider path is falling back.
7. Confirm whether the failure is:

   * missing key
   * wrong key
   * exhausted quota
   * wrong model
   * wrong endpoint
   * bad request payload
   * retry loop causing quota/rate failure
   * frontend always selecting fallback
   * backend health check incorrectly reporting green
8. Report the exact root cause before final completion.

Live AI provider fix:

1. Do not hardcode one Gemini model in multiple places.
2. Create one shared AI provider config, for example:

   * AI_PROVIDER
   * GEMINI_API_KEY
   * GEMINI_MODEL
   * GEMINI_FALLBACK_MODEL if supported
3. The assistant backend must use that one shared config.
4. Add a small provider canary/test function that makes a minimal live AI request and confirms the provider is actually usable.
5. `/api/ai/health` must not simply say configured=true because a key exists.
   It must distinguish:

   * configured
   * live provider reachable
   * quota exhausted
   * fallback active
   * provider failed
6. If Gemini returns 429 / RESOURCE_EXHAUSTED, do not keep retrying aggressively.
   Use safe retry/backoff and then switch to local fallback.
7. If the provider has no quota available, the app must say that honestly, but the system must still be ready to work immediately once a valid key/quota/model is available.
8. Do not present fallback as if the AI is fully working.
   The UI must clearly show:

   * “Live AI active” when provider is working
   * “Fallback mode” only when provider quota/key/model is unavailable

Important:
If external Gemini quota is exhausted, code alone cannot create more quota. But the app must prove whether the provider is actually exhausted, misconfigured, or being called incorrectly. Fix the integration if broken. If the key/quota is the only blocker, report that clearly and leave the app correctly wired so that adding a valid key/quota immediately activates live AI.

Assistant behavior:
When live AI works:

* It must answer the user’s question using BreedLog records as context.
* It must not hallucinate outside the available records.
* It must explain what data it used.
* It must give practical farm-management insight, not only raw counts.
* It must remain read-only.

When live AI fails:

* Use local record fallback.
* The fallback must answer the actual selected question, not always the same generic herd overview.
* “Which ram is performing best?” must produce ram/sire ranking from available data.
* “What needs my attention first?” must produce a priority list.
* “How complete is my herd data?” must produce data-quality findings.
* “Explain lamb growth performance” must use lamb/weight data where available and clearly state missing data if incomplete.
* “Data Quality” must list missing fields and improvement actions.

Mobile assistant UI fix:
The assistant modal must be rebuilt as a proper mobile layout, not patched.

Required layout:

1. Modal container:

   * width: 100vw max safe width
   * max-width: 100vw
   * height: 100dvh or near-full mobile height
   * no horizontal overflow
   * safe-area support
2. Header:

   * fixed/sticky at top inside modal
   * close button always visible and tappable
3. Body:

   * scrollable message area
   * response cards must never be hidden behind the input bar
   * add bottom padding equal to input composer height
   * long text wraps
   * long words/JSON cannot overflow
4. Input composer:

   * sticky/fixed at bottom inside modal
   * visible above browser safe area
   * send button clear and tappable
   * send icon visible
   * textarea does not push outside viewport
5. Suggested question chips:

   * wrap or horizontal scroll cleanly
   * no layout widening beyond viewport
6. Response card:

   * compact on mobile
   * use proper line-height and spacing
   * do not create a huge block that gets clipped
   * “Data used” and “Follow up” sections must remain inside the scrollable body

Fix the current clipping:
In the screenshot, the bottom input area overlays/cuts the assistant answer card. That is not acceptable. The response area must scroll above the input composer.

Refresh/sync widget fix:
The refresh/sync chip is still visually interfering with the BreedLog logo/header area on mobile.

Required:

1. Remove any mobile absolute/fixed placement that overlays the logo card.
2. Put sync status in a proper header/status row.
3. It must not cover:

   * BreedLog logo
   * farm name
   * page heading
   * cards
   * action buttons
   * assistant modal
4. Test on 360px, 390px, and 430px mobile widths.
5. It must look intentionally placed, not floating over branding.

Dark-mode readability:
Keep enforcing:

* all dark-mode buttons must have white/high-contrast text
* disabled buttons must still be readable
* gradient buttons must not use dark text
* icons must remain visible

Testing/proof requirements:
Add or update tests/checks for:

AI backend:

* provider config loads from one shared place
* health endpoint distinguishes configured/live/quota/fallback states
* Gemini 429 triggers fallback without raw JSON
* live provider failure does not crash assistant
* fallback answers are category-specific, not one generic answer
* raw provider JSON is never rendered to the user

AI frontend:

* mobile assistant has no horizontal overflow
* response card is not clipped behind input composer
* input composer stays visible and usable
* send button is visible in dark mode
* suggested questions do not overflow screen

UI layout:

* sync/refresh widget does not overlap logo/header on 360px, 390px, 430px widths
* dark-mode action buttons have readable text

Run and report:

* exact root cause of live AI failure
* whether it is quota, config, endpoint, model, retry, or code logic
* exact files changed
* AI provider config location
* AI health endpoint behavior
* fallback implementation location
* mobile assistant layout files changed
* sync widget layout files changed
* tests added/updated
* npm run check result
* npm run build result
* provider canary result if possible
* mobile viewport verification result

Acceptance gate:
Do not mark this complete unless:

1. Live AI path is correctly wired and proven, or the only remaining blocker is honestly confirmed external provider quota/key availability.
2. The app automatically falls back to local record answers without showing raw errors.
3. The fallback answers the actual question category, not only generic herd summary.
4. The assistant modal is fully usable on mobile with no clipping behind the input bar.
5. The sync/refresh widget no longer overlaps the BreedLog logo/header.
6. Dark-mode buttons remain readable across the whole app.

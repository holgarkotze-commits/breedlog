REPLIT AGENT DIRECTIVE: FIX CRITICAL SYNC & ROUTING LOCKS
Subject: Emergency Fix for Animal Profile Routing and Sync-Persistence (Web App) Status: Production Blocker

1. The Root Cause Analysis
The "Animal Not Found" Loop: When clicking an animal card, the router navigates to /animals/[ID]. If that ID exists only in a local pending queue and the Profile Page only queries the remote Database, the lookup fails, resulting in the blank "Animal not found" screen.

Navigation Lock: The "Back" button failure indicates the router is stuck in an error-state loop or a broken history.push stack.

2. Immediate Engineering Requirements
A. Hybrid Data Fetching (Local + Cloud)
Modify the Animal Profile Page (/animals/:id) logic to check both sources:

First: Check the PendingSyncQueue (IndexedDB/LocalStorage). If the ID is found there, load the profile from local data immediately.

Second: If not in the queue, fetch from the remote Database.

Result: This ensures that even if an animal isn't synced yet, its profile remains viewable and editable.

B. Fix the Navigation "Dead-End"
Global Error Boundary: Implement a functional Error Boundary on the Animal Profile route. If "Animal not found" triggers, provide a "Return to My Herd" button that performs a window.location.href = '/my-herd' to force-reset the navigation stack.

Back Button Fix: Ensure useNavigate() or router.back() is correctly bound and not intercepted by unhandled sync-error states.

C. Force-Sync Recovery Mechanism
The "Sync complete with warnings" and "Data saved locally" errors show the background worker is hanging.

Implement "Hard Sync" Button: Add a button in the Settings or Sync footer that performs a Force Clear & Resync.

This must: 1. Export all local pending JSON to a log. 2. Clear the stuck Service Worker cache. 3. Re-attempt a bulk UPSERT to the database.

📋 PASTE THIS PROMPT INTO REPLIT AGENT
Agent: The BreedLog Web App has critical navigation and sync failures. Fix the following immediately:

ANIMAL NOT FOUND ERROR: When a user clicks an animal, the app goes to a blank screen.

Requirement: Update the AnimalProfile component to fetch data from the LOCAL sync queue if the remote DB query fails or is pending. The UI must never show "Animal not found" for a record that was just created locally.

BROKEN BACK BUTTON: The blank screen prevents the user from returning to the previous page.

Requirement: Implement a robust catch block on the route. If the ID is invalid, automatically redirect the user back to /my-herd instead of locking them on a blank screen.

RESILIENT SYNC: The app shows "Sync issue - data saved locally".

Requirement: Debug the service-worker or sync-provider. Ensure that navigator.onLine is verified by a real fetch ping to the API. If online, the app MUST attempt to push the local queue every 30 seconds until the queue is empty.

DEBUG LOGS: Add a visible "Debug Sync" button in the footer that displays exactly WHICH animal ID is failing to sync and the specific Server Error (e.g., 400 Bad Request, 403 Forbidden).

Goal: 100% navigation uptime. No blank screens. Local animals must be viewable in profile mode immediately after creation.
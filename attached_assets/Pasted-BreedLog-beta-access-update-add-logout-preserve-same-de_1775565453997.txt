BreedLog beta access update — add logout, preserve same-device reuse, and enforce one desktop + one mobile per code

You must make only the narrowly scoped changes described below. Do not redesign or alter unrelated BreedLog features, business logic, animal flows, reporting, styling, navigation structures, or data models except where strictly necessary for logout, device-slot handling, and same-code dual-device activation.

Current confirmed state:
The previous “code not found” issue was partly caused by using the wrong public URL. Using the correct live URL, activation now works. However, the current invite code logic still behaves incorrectly for the intended tester model.

Observed issue now:
A code can be used successfully on desktop, but when trying to use that same code on mobile, the system says the code has been fully used and asks for a new code. That is not the intended behavior.

Required intended behavior:
One code must be usable on:

* one desktop device
* one mobile device

Those two must be allowed at the same time under the same tester/workspace/data.
The same code must not work on:

* a second desktop
* a second mobile phone

Also required:
Users need a visible logout option in both desktop and mobile layouts.
After logout on a previously approved device, the user must be able to log back in again on that same device using the same code, without the device being treated as a second new device.
Logout must not delete any animal data or local farm data.
Logout must only end the session, not destroy the linked workspace or device registration unless explicitly reset by admin.

What must be fixed:

1. Add a proper logout action for users in both desktop and mobile UI.
   Put it in a sensible place accessible from the main app shell/navigation.
   It must work on both layouts.

2. Adjust the beta access logic so that one code supports exactly:

   * one desktop slot
   * one mobile slot

3. If desktop slot is already occupied by a recognized approved desktop device, that same device must still be able to log out and log back in again with the same code.

4. If mobile slot is already occupied by a recognized approved mobile device, that same device must still be able to log out and log back in again with the same code.

5. Only a different second desktop must be rejected.

6. Only a different second mobile phone must be rejected.

Important logic rule:
The system must distinguish between:

* same approved device re-authenticating
  and
* a new extra device trying to consume a slot

A same-device re-login must not be blocked with “code fully used.”

Implement or verify:

* device fingerprint / registration logic that is stable enough to recognize the same approved device after logout
* session logout separate from device slot reset
* code redemption rules that check slot type and device identity before rejecting
* workspace/data continuity after logout/login
* no deletion of user data on logout

User experience requirement:
If a slot is already occupied by the same device, allow login.
If a slot is occupied by another device of that same class, show a clear message such as:

* “The desktop slot for this code is already assigned to another device.”
* “The mobile slot for this code is already assigned to another device.”
  Do not show generic “fully used” messaging when the real issue is slot-specific.

You must test this thoroughly on the live effective system, not just internal preview.

Required tests:
Test 1:
Create a fresh code.
Use it on desktop.
Confirm activation succeeds.

Test 2:
Use that exact same code on one mobile device.
Confirm activation also succeeds.
This is mandatory.

Test 3:
Confirm both desktop and mobile now point to the same tester/workspace and can sync shared data.

Test 4:
Create sample BreedLog data on desktop, sync if needed, confirm it appears on mobile.

Test 5:
Create sample BreedLog data on mobile, sync if needed, confirm it appears on desktop.

Test 6:
Logout on desktop.
Login again on that same desktop with the same code.
Confirm it succeeds and no data is lost.

Test 7:
Logout on mobile.
Login again on that same mobile device with the same code.
Confirm it succeeds and no data is lost.

Test 8:
Try same code on a second desktop.
Confirm rejection with desktop-slot-specific message.

Test 9:
Try same code on a second mobile.
Confirm rejection with mobile-slot-specific message.

Test 10:
Confirm logout does not wipe local or cloud-linked data.

Test 11:
Confirm admin reset of a slot is still possible without deleting farm data.

Test 12:
Confirm all of this works from the real published live URL used by testers.

Required deliverable back:
Provide a short final report stating:

* exact files changed
* exact logic changed for logout and same-device recognition
* how one-desktop + one-mobile enforcement now works
* where logout appears in desktop and mobile UI
* whether same-device relogin with same code now works
* results of all 12 tests
* the exact public URL that was tested
* confirmation that no unrelated BreedLog functionality was changed

Again: do not make unrelated changes. Only implement logout, same-device re-login, and one-desktop + one-mobile same-code behavior with full testing.

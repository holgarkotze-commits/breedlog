YOU ARE WORKING ON PRODUCTION. READ THIS CAREFULLY.

NON-NEGOTIABLE RULE: ✅ Every change MUST be applied to PRODUCTION AND MUST BE VERIFIED ON THE PUBLISHED URL: https://breedlog.replit.app NOT in the dev preview. NOT in “Run”. NOT “it works locally”. PRODUCTION ONLY.

If you make a change and it is not visible/working on https://breedlog.replit.app after publishing, then the task is NOT DONE.

======================== GOAL Fix and harden the Beta Access / Invite Codes system so:

Single Source of Truth: All codes live in the PRODUCTION DB.

Reliable Auth: Testers verify via Token (not cookies).

No Stale Data: Admin and Client bypass aggressive Service Workers for data.

======================== MANDATORY IMPLEMENTATION REQUIREMENTS

1) SCHEMA & MIGRATION (FIRST STEP)

Before modifying UI, ensure the Database Schema supports: note (string), expiresAt (datetime), and maxUses (int).

Action: Verify schema and run necessary migrations against the PRODUCTION DB immediately so the backend does not crash.

2) PRODUCTION IDENTITY (The "Hard Lock")

Ensure ALL environment variables (DATABASE_URL) point to PRODUCTION.

Add a visual indicator in /admin:

If running locally/dev: Show "⚠️ DEVELOPMENT MODE".

If running on published URL: Show "✅ PRODUCTION MODE".

Display the Production DB Host (masked) and Row Counts in Admin to prove we are looking at real data.

3) AUTHENTICATION: TOKEN OVER COOKIES

Problem: Mobile browsers drop session cookies often.

Fix:

On activation, generate a permanent deviceToken.

Store this in localStorage.

REQUIRED: All API calls must attach Authorization: Bearer <token>.

Backend must validate this Bearer token, NOT the cookie.

4) ADMIN CONTROLS (CRUD)

Create: Add Invite Code (Inputs: Code, Note, Expiry).

Revoke: Mark a used code as inactive (keep history).

Delete: Permanently remove an unused code (cleanup).

Max Testers: Add an editable setting in Admin to change the limit (e.g., increase from 50 to 100 without code pushes).

5) CACHE DESTRUCTION

Service Worker: Must strictly exclude /admin/* and /api/* routes.

API Headers: All Admin/Device API responses must include: Cache-Control: no-store, no-cache, must-revalidate

Client Versioning: Add a version.json endpoint. On app startup, client checks version. If client version != server version, force a hard reload (window.location.reload(true)).

6) LAMBS EXPORT

Add the missing "Export PDF" button to the Lambs section (mirroring Rams/Ewes functionality).

======================== EXECUTION PLAN (DO NOT SKIP)

PHASE 1: DB & BACKEND

Update Schema (add missing columns).

Update Backend API to require Bearer Token.

Add the db-info endpoint.

TEST: Verify DB connection is Production.

PHASE 2: FRONTEND & WORKER

Update Service Worker to ignore API routes.

Update Client logic to store/send Bearer Token.

Build Admin UI (Identity Block + Create/Revoke/Delete).

Add Lambs PDF Export.

PHASE 3: PUBLISH & VERIFY

Publish to Replit Production.

Verify on https://breedlog.replit.app.

======================== ACCEPTANCE CRITERIA (PASSS/FAIL) You cannot mark this task done until you provide:

Screenshot of Production Admin: Showing "✅ PRODUCTION MODE" and the correct DB row counts.

Live Test: I will create a code "PROD_TEST" in Admin, and immediately use it on my phone to activate. If it fails, you fail.

Cache Test: I will revoke a code, refresh my phone, and it must immediately be rejected.

Lambs PDF: The button must work on Production.

START NOW. EXECUTE PHASE 1.
REPLIT TASK: BreedLog – Beta Access + Admin Panel must be 100% production-consistent (NO dev-only fixes).

1) Non-negotiable goal

All beta access + invite codes must behave identically on:

Published URL (production) and dev preview

Mobile + Desktop

Online + offline-first usage (where applicable)

No more “works in dev only”. Every fix must be validated on breedlog.replit.app after publish.

A) Fix the “old codes still showing” problem (production consistency)
Problem

After republish, admin still shows old/stale codes. We previously had cache/service worker/API caching issues causing mismatch between UI and DB.

Required changes

Single source of truth: ensure ALL admin code list reads from the production database at runtime.

Add a DB identity block (already present) but make it actionable:

Show: environment: production|development

Show: db host / db name (masked), codes_count, activations_count

Add a “Hard Refresh Data” button in Admin that does:

Calls admin list endpoint with cache: 'no-store'

Adds headers Cache-Control: no-store, no-cache, must-revalidate, Pragma: no-cache

Forces React Query / fetch cache busting (append ?t=${Date.now()})

Service worker rules (critical)

Admin routes must never be cached:

/admin

/api/admin/*

/api/beta/*

/api/device/*

If a service worker exists, implement network-only for those routes.

If unsure, simplest: bypass SW entirely for admin + beta routes.

Server must send no-cache headers on:

/api/admin/*

/api/beta/*

/api/device/*

B) Invite codes must ALWAYS work (no “Device not registered/authenticated” loops)
Problem seen by testers

401 Device not registered

500 Device not authenticated
This happens when device token/session isn’t reliably saved/sent on mobile browsers.

Required design (do this cleanly)

Use token-based auth for device access (NOT session-cookie dependent).

On successful activation:

Server returns deviceToken

Client stores token in localStorage (and optionally indexedDB fallback)

ALL protected requests include header: Authorization: Bearer <deviceToken>

Activation endpoint must be idempotent:

If device already activated with that code, return same valid token (do not error)

Add robust handling for duplicate device creation race conditions:

If unique constraint hit, fetch existing device row and continue.

Add a clear error message for invalid code vs exceeded capacity vs revoked:

Invalid code: Code not found

Code used up: Code already fully used

Capacity reached: Tester limit reached

C) Admin must control tester capacity + codes properly
Required UI/Features

Replace hard-coded tester cap with an Admin setting:

Max testers editable in Admin (default 50, but I can set to 20 or any number)

Stored in DB (settings table), not env-only

Invite codes management:

Must show full list (no stale data)

Add Delete code option (only if code has 0 activations)

If code has activations, allow only Revoke (soft disable), not delete

Code creation:

When Admin clicks “Create Code”, backend must:

create code

immediately read it back from DB

return it to UI

UI then refreshes list from DB (no local optimistic-only list)

D) IMPORTANT: Production vs Dev mismatch must be impossible
Required implementation check

Confirm how DB connection is selected in:

dev preview

published production

Ensure production uses ONE configured DATABASE_URL and that Admin shows it as “production”.

If Replit uses different env vars for deployments:

Ensure deployment env has correct DATABASE_URL

Confirm migrations run for production too

Add endpoint /api/admin/db-info (if not already) returning:

env name

db host/name (masked)

codes count

activations count

last code created timestamp

E) Must include missing “LAMBS export PDF” button (separate issue)

On “My Herd” screen:

Rams has Export PDF ✅

Ewes has Export PDF ✅

Lambs export PDF is missing ❌

Add Export PDF button for Lambs exactly like Rams/Ewes.

Acceptance tests (MUST be proven on published URL)

After publish to breedlog.replit.app:

Admin tests (desktop Chrome)

Go to /admin, enter PIN BL3666

Codes list loads and matches DB count in db-info

Create a new code → it appears immediately after refresh and persists after page reload

Delete an unused code → removed from DB and list

Revoke a used code → cannot be used again for activation

Tester tests (mobile + desktop)

On fresh device/browser, enter valid invite code

Device activates successfully (no 401/500)

Refresh page → still has access (token persisted)

Try invalid code → correct “Code not found”

Try used-up code → correct “Code fully used”

If tester limit reached → correct “Tester limit reached”

Cache tests

Create code in admin

Open admin in another browser/incognito

The new code appears without showing stale/old list

Hard Refresh Data button forces correct list

Deliverable

Commit changes

Publish

Provide screenshots from published URL showing:

db-info block (production)

codes list matches db count

successful mobile activation

Do NOT mark complete until all acceptance tests pass on production.
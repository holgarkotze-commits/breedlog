Update the Breeding tab “Record Event” flow.

1) Record Event Type Selector
In Breeding > Record Event, add an event type selector (dropdown or segmented control):
- Mating Groups
- Flock Health

Selecting an option must open the relevant event form.

==================================================
2) FLOCK HEALTH EVENT (NEW)
==================================================

Create a new “Flock Health” event entry system where I can record herd treatments (vaccines, supplements, medicines).

A) Event capture UI
- Event Title: “Flock Health Treatment”
- Event Date (date picker) [required]
- Product Name (text) [required]
- Route (dropdown) [required]:
  - Intravenous
  - Intramuscular
  - Subcutaneous
- Notes (optional)

B) Animal selection
Add a “Treat Which Animals?” selector:
- Treat All Active Animals
- Select Individual Animals

If “Select Individual Animals”:
- Show a searchable multi-select list/table of active animals (Rams/Ewes/Lambs)

C) Treatment Record Table (must be table form)
When saving the event, generate a table of treatment rows.

Heading: “Animal Health Treatment Record”

TABLE COLUMNS:
1. Date
2. Animal ID
3. Product Name
4. Qty Given (ml)  ← numeric dropdown (e.g., 1–50 ml) or numeric input with step 0.5
5. Route (IV/IM/SC as dropdown)
6. Notes (optional)

RULES:
- If “Treat All Active Animals” is selected, the table automatically creates one row per active animal.
- If “Select Individual Animals”, table rows are created only for selected animals.
- User can edit Qty per animal (and optional notes) before final save.
- Save the full event + rows to the database and ensure OFFLINE support matches existing PWA rules.

==================================================
3) PDF EXPORT (MUST-HAVE)
==================================================

Add “Export PDF” for every saved Flock Health event.

PDF Requirements:
- Use the BreedLog standard PDF formatting used in the app:
  - header, logo, footer unchanged
  - clean, aligned table
  - compact spacing (no stretched rows)
  - A4
  - portrait
- PDF Title: “Flock Health Treatment Record”
- Include event summary at top:
  - Event Date
  - Product Name
  - Route
  - Treated: All Active Animals OR Selected Animals (count)
- Include the treatment table below

==================================================
4) RECORDS FILING (AUTO)
==================================================

After a Flock Health event PDF export:
- Automatically save/file the PDF in:
  Records → Exported Documents → Flock Health
Also create/ensure there is a folder:
  Records → Flock Health
where the list of Flock Health events and their PDFs can be accessed.

Each event should be accessible later with:
- View event
- Export PDF again
- View saved PDF

Implement clean UI and confirm it works offline (event creation, viewing cached events, and later sync).

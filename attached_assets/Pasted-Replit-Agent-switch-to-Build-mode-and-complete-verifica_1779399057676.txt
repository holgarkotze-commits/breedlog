Replit Agent, switch to Build mode and complete verification/fix properly.

Do not verify by old Codex hashes anymore. PR #8 was merged and hashes changed in GitHub/Replit. Verify by current HEAD, files, features, and tests.

Do not publish.
Do not run U2A2ZAVQ reset/reseed.
Do not change data.

Current issue:

* Replit reports HEAD 1fd60b4
* git status clean
* reset script exists
* npm run check/build were blocked because you were in Plan mode
* npm run test:cert showed 9 failures. That is not acceptable even if exit code returned 0.

Now run in Build mode:

1. Confirm current code:
   git rev-parse --short HEAD
   git status --short
   git log --oneline -8
   test -f scripts/reset-master-simulation-workspace.ts && echo RESET_SCRIPT_PRESENT

2. Confirm these files/features exist:
   test -f tests/ai-readiness-phase6.test.ts && echo PHASE6_TEST_PRESENT
   test -f tests/mobile-tablet-proof-phase7.test.ts && echo PHASE7_TEST_PRESENT
   test -f tests/access-master-simulation-phase5e.test.ts && echo PHASE5E_TEST_PRESENT
   grep -R "U2A2ZAVQ" -n server shared scripts tests | head -20
   grep -R "KWANTAM_SIMULATION_2022_TO_2026_V1" -n server shared scripts tests | head -20

3. Run exact targeted tests:
   npx tsx --test tests/access-master-simulation-phase5e.test.ts
   npx tsx --test tests/ai-readiness-phase6.test.ts
   npx tsx --test tests/mobile-tablet-proof-phase7.test.ts

4. Run full gates:
   npm run check
   npm run build
   npm run test:cert

5. If npm run test:cert still shows failures, extract the exact failed test names and fix only the failures caused by PR #8 / merged build. Do not rewrite unrelated systems. Do not weaken tests. Do not delete tests to make green.

6. After fixes, rerun:
   npm run check
   npm run build
   npm run test:cert

Final report only:

* HEAD
* git status
* reset script present YES/NO
* Phase 5E/6/7 tests present YES/NO
* targeted Phase 5E result
* targeted Phase 6 result
* targeted Phase 7 result
* npm run check PASS/FAIL
* npm run build PASS/FAIL
* npm run test:cert PASS/FAIL with exact pass/fail/skipped count
* commits made if fixes were required
* working tree clean YES/NO

Stop there. Do not publish. Do not apply U2A2ZAVQ reset.

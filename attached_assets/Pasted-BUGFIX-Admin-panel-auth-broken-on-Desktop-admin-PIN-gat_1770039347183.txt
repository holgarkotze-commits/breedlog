BUGFIX: Admin panel auth broken on Desktop + admin PIN gate inconsistent.

CURRENT BEHAVIOR (REPRO STEPS):
1) Desktop (Chrome/Edge): Navigate to /admin
2) Enter PIN: BL3666
3) Press Enter OR click “Access Admin Panel”
RESULT: It reloads/redirects back to the same Admin PIN login page (infinite loop).
This still happens after republish AND in Incognito.

MOBILE BEHAVIOR (SECURITY BUG):
On mobile, sometimes /admin opens directly without prompting for PIN (bypasses admin gate).
Logout button now exists and works, but PIN gate must ALWAYS be enforced unless an active admin session exists.

REQUIREMENTS (MUST IMPLEMENT):
A) Desktop admin login must work:
- After entering correct PIN, user must be routed to admin panel and STAY there on refresh.
- No full page reload loops.
- Works on Desktop Chrome + Edge + Firefox, normal + incognito.

B) Admin PIN gate must be consistent:
- /admin must ALWAYS show PIN screen unless an admin session is valid.
- Do NOT bypass PIN on mobile unless admin session is valid.
- Admin session storage must be identical across mobile/desktop (same key + same check).

C) Admin session persistence:
- Store admin auth flag/token in localStorage (preferred) with:
  - key: "breedlog_admin_authed" = "true"
  - key: "breedlog_admin_authed_at" = ISO timestamp
  - expiry: 24 hours (after that force PIN again)
- Route guard logic must read EXACTLY these keys and enforce expiry.
- Logout must clear both keys and redirect to /admin (PIN screen).

D) Prevent form-submit reload:
- Ensure PIN submit handler uses preventDefault and does not cause a full page reload.
- Use client-side navigation (React router navigate) after success.

E) Add diagnostics (TEMPORARY but helpful):
- Add console logs (only in dev or behind a flag) showing:
  - When login succeeds
  - What keys were written
  - What the guard reads and why it redirects
- If there are JS errors on desktop, fix them (do not ignore).

ACCEPTANCE TESTS (MUST BE DONE, NOT CLAIMED):
1) Desktop: /admin → enter BL3666 → admin opens → refresh page → remains in admin
2) Desktop Incognito: same as above
3) Mobile: /admin → must show PIN if logged out; after PIN it opens; after logout it returns to PIN
4) Wrong PIN: shows error and stays on PIN screen
5) Expiry: if authed_at older than 24h, force PIN again

DELIVERABLE:
- Tell me exactly which files were changed (e.g. AdminLogin.tsx / AdminGuard.tsx / routes)
- Confirm each acceptance test as PASS.
DO NOT change unrelated features.

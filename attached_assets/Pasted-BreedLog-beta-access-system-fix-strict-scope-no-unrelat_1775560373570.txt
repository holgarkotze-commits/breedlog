BreedLog beta access system fix — strict scope, no unrelated changes

You must fix only the beta access / activation / tester identity / device-linking / persistence flow described below. Do not redesign the app, do not refactor unrelated modules, do not touch any existing BreedLog business logic, animal records, UI sections, reporting, navigation, or styling unless a change is strictly required to solve this exact issue. Any code change must stay narrowly scoped to the beta-access and sync/persistence layer requested here.

Current issue:
From the admin panel, newly created invite codes show as Active, with valid expiry dates and remaining uses, but when entered on the desktop app or mobile app, the client gets “Code not found. Please check and try again.” and “No activation found.” This means the admin-facing code creation and the client-facing code redemption are not reading the same truth source, or the lookup/normalization/validation pipeline is broken.

You must investigate and fix the root cause properly, not patch it superficially.

What to investigate first:
Check whether the admin panel and the client activation screen are connected to the exact same backend project, database, environment variables, and tables. Confirm there is no mismatch between dev/prod environments, preview/live endpoints, duplicated databases, wrong API URL, wrong project ID, wrong auth/storage namespace, or stale local config. Confirm that the code created in admin is actually persisted where the activation endpoint expects to find it.

Then check the entire invite-code flow end to end:
admin creates code → code is stored → code is retrievable by activation API → code comparison is normalized properly → code status is validated → usage rules are enforced → activation record is created → client device is bound → access is remembered locally and remotely.

Specific bug risks to check:
Code normalization problems such as uppercase/lowercase mismatch, trimmed spaces, hidden characters, hyphen stripping, frontend formatting differences, or accidental hashing/encoding mismatch.
Date validation problems where a code is marked active in admin but rejected during redemption because of timezone or expiry parsing differences.
Status mismatch where the admin label says Active but backend query filters reject the record.
Uses counter logic incorrectly marking code as unavailable.
Client app calling the wrong API route or wrong database row shape.
Admin panel writing to one table while the app reads from another.
RLS / permissions / backend policy issue preventing public redemption lookup.
PWA/mobile installed app using stale cached API bundle or stale env config.
Published desktop/mobile client not refreshed after backend schema changes.

Required functional outcome:

1. If an admin creates a valid code, that code must be redeemable immediately on the client.
2. When a user activates successfully, that user must not be asked to re-enter the code every time.
3. A successful activation must survive normal app restarts, browser closes, device restarts, and offline periods where applicable.
4. Activation must remain valid for the allowed active period.
5. Existing local animal data and client-entered data must never be deleted just because a code expires.
6. If the tester later receives renewed access, the tester must be able to continue with the same existing farm data and not start from zero.
7. One tester account/code must support one desktop device and one mobile device only.
8. Those two approved devices must sync to the cloud and see the same data after sync.
9. The same code must not be usable on multiple desktops or multiple phones beyond the allowed pair.
10. If the user reinstalls the app, there must be a defined recovery path for their prior cloud-backed data.

Implement the access model like this:
Treat the invite code as an onboarding key, not the permanent identity itself. Once redeemed, create or bind a persistent tester identity/account record. The code grants access and links the tester to a cloud-backed tenant/profile/workspace. After that, ongoing access should rely on the persistent tester identity plus device registrations, not repeated manual code entry every session.

Required device policy:
One redeemed code may authorize exactly two device slots under one tester identity:
one desktop slot
one mobile slot

The system must identify device class reliably enough to enforce this. A redeemed code should not activate a second desktop if the desktop slot is already occupied, and should not activate a second phone if the mobile slot is already occupied, unless an admin explicitly resets or revokes that slot.

Required persistence rules:
Local app data on device must remain intact unless the user explicitly wipes the app or installs on a brand-new device.
Cloud sync must become the authoritative recovery path for reinstalls and device replacement.
If a user reinstalls on the same allowed device, provide a recovery/rebind path that restores their linked cloud data without making them lose their farm records.
If a code expires, local data should remain visible locally unless product rules intentionally block editing; however, cloud-linked data must not be destroyed.
If renewed access is granted later, the same tester should reconnect to the same cloud dataset.

Important architectural direction:
Do not make the code itself the long-term container of the user’s data.
The code should only unlock and bind a tester identity/workspace.
User data must live under the persistent tester/workspace layer and sync through that layer.

You must implement or verify all of the following:
A proper invite_codes table or equivalent authoritative source.
A proper activations/tester_accounts/workspaces table structure or equivalent.
A device_registrations structure that records desktop slot and mobile slot separately.
A durable local session/persistence mechanism.
A cloud sync link from the tester/workspace to the user’s BreedLog data.
A safe reactivation/recovery flow for reinstall scenarios.
An admin reset option for replacing a lost desktop or lost phone without deleting farm data.

Do not broaden scope beyond what is necessary. Do not alter existing BreedLog features unrelated to access, activation, sync, device policy, or data persistence.

Testing you must run after the fix:
Test 1: Create a brand-new code in admin. Redeem it immediately on desktop. It must work first time.
Test 2: Use the same code on one mobile device. It must work and bind the mobile slot.
Test 3: Attempt same code on a second desktop. It must be rejected with a clear slot-limit message.
Test 4: Attempt same code on a second mobile phone. It must be rejected with a clear slot-limit message.
Test 5: Close and reopen both approved devices. Neither should ask for the code again.
Test 6: Add sample BreedLog data on mobile, sync, then open desktop and confirm the same data appears.
Test 7: Add sample BreedLog data on desktop, sync, then confirm it appears on mobile.
Test 8: Expire the code or simulate expiry. Confirm existing local/cloud data is not deleted.
Test 9: Renew access for the same tester. Confirm the same data remains linked.
Test 10: Reinstall one approved device and verify recovery/rebind to the same cloud-backed dataset.
Test 11: Confirm code lookup works regardless of case and strips accidental whitespace safely.
Test 12: Confirm no stale cache/service worker/build issue causes newly created codes to fail on installed clients.

Deliverables required from you:
First, identify the exact root cause of why active codes are showing “Code not found.”
Second, apply the narrow fix.
Third, implement the persistent activation + one-desktop/one-mobile binding model if not already present.
Fourth, implement or verify cloud sync continuity so tester data survives expiry/reinstalls.
Fifth, run the tests above.
Sixth, provide a short final report containing:
the exact root cause
the exact files changed
the exact database/schema/policy changes made
how desktop/mobile slot enforcement now works
how reinstall recovery now works
the results of each test
confirmation that no unrelated BreedLog functionality was changed

Again: do not make any unrelated code changes. Only fix the invite code redemption, activation persistence, device-slot enforcement, and cloud sync/data continuity flow described here.

# XLSX Dependency Blocker

BreedLog requires true Excel `.xlsx` import/export support.

In this environment, XLSX implementation is blocked because npm registry package access returned `403 Forbidden` for XLSX libraries.

CSV remains a temporary fallback only.

XLSX must be completed later with a real maintained XLSX library.

No fake XLSX support was implemented.

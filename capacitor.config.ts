import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stitchworx.breedlog',
  appName: 'BreedLog',
  webDir: 'dist/public'
};

export default config;
